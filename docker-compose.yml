version: '3.8'

services:
  app:
    build: .
    expose:
      - "3000"
    volumes:
      - data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000

volumes:
  data:
    driver: local
